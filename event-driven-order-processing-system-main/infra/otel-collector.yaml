receivers:
  otlp:
    protocols:
      http:

exporters:
  otlp/jaeger:
    endpoint: minishop-jaeger:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp/jaeger]
